export const environment = {
  production: true,
  // Render assigns this URL from the service name you choose when creating the web service
  // (Host: https://<service-name>.onrender.com). Update this if you name it differently.
  apiBaseUrl: 'https://freshomill-api.onrender.com',
};
